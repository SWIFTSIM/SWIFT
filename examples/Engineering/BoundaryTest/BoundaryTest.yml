#This test works only with Euler fluid SPH with weakly compressible equation of state and exact cell updating

# Define the system of units to use internally. 
InternalUnitSystem:
  UnitMass_in_cgs:     1000   # Grams
  UnitLength_in_cgs:   100   # Centimeters
  UnitVelocity_in_cgs: 100   # Centimeters per second
  UnitCurrent_in_cgs:  1   # Amperes
  UnitTemp_in_cgs:     1   # Kelvin

# Parameters governing the time integration
TimeIntegration:
  time_begin: 0.    # The starting time of the simulation (in internal units).
  time_end:   2.048   # The end time of the simulation (in internal units).
  dt_min:     1e-6  # The minimal time-step size of the simulation (in internal units).
  dt_max:     1e-3  # The maximal time-step size of the simulation (in internal units).

# Parameters governing the snapshots
Snapshots:
  basename:            boundary_test_result # Common part of the name of output files
  time_first:          0.       # Time of the first output (in internal units)
  delta_time:          0.001      # Time difference between consecutive outputs (in internal units)
  compression:         1
  
# Parameters governing the conserved quantities statistics
Statistics:
  delta_time:          1e-2 # Time between statistics output

# Parameters for the hydrodynamics scheme
SPH:
  resolution_eta:        1.2348   # Target smoothing length in units of the mean inter-particle separation (1.2348 == 48Ngbs with the cubic spline kernel).
#  resolution_eta:        1.078734   # Target smoothing length in units of the mean inter-particle separation (1.078734 == 32Ngbs with the cubic spline kernel).
  CFL_condition:         0.1      # Courant-Friedrich-Levy condition for time integration.
  h_max:                 0.13
  h_min:                 0.13

# Parameters related to the initial conditions
InitialConditions:
  file_name:  ./boundary_test.hdf5       # The file to read
  periodic:   1

EoS:
  soundspeed: 221.47
  #soundspeed: 350.00
  density_reference: 1000.0
