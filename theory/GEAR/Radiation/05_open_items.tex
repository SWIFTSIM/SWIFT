\section{Open items}
\label{sec:open}

\textbf{Restarted 2026-07-20.} This list previously mixed genuine open
task-graph/algorithm work with validation-run tracking (results
against Hu et al.\ 2017/Smith et al.\ 2021/STARBENCH, and the e10
diagnostic history). Validation status now lives exclusively in
Section~\ref{sec:validation} (its own summary table,
Table~\ref{tab:validation-targets}); every settled bug fix or shipped
feature that had accumulated here was moved to
Section~\ref{sec:bugs} or the relevant part of Section~\ref{sec:algorithm}/
\ref{sec:taskgraph}. This list is renumbered from T1 and contains only
currently-open task-graph/algorithm items -- nothing here tracks
validation-against-a-paper status.

\begin{itemize}
  \item[$\square$] \textbf{T1} -- Confirm or rule out the
    \code{radiation\_level} orphaned-link risk
    (Section~\ref{sec:not-regular}) on the clump examples specifically.
    It requires two neighbouring regions' \code{hydro.super} to land
    at different tree depths, which uniform-density ICs never trigger
    but a $100\times$ density clump plausibly could. Rebuild with
    \code{--enable-debugging-checks} and run
    \code{StromgrenSphereClumpSmith2021} at a resolution fine enough
    to actually split the tree near the clump boundary; the
    \code{error()} at \code{cell.c:1387} will fire if it's hit.

  \item[$\square$] \textbf{T2} -- MPI is explicitly not supported yet
    for the radiation loop (\texttt{error("MPI is not yet
    implemented")} in both \code{task.c} and \code{cell\_unskip.c}
    for \texttt{nr\_nodes > 1}). Not a bug, just an explicit gap to be
    aware of before attempting a multi-node run with this feedback
    channel active.

  \item[$\square$] \textbf{T3} -- The \code{StromgrenSphere} D-type
    growth-curve diagnostic (an algorithm-behaviour check against the
    classical Spitzer formula, not a validation target) has an
    unexplained residual: even with the search-radius-expansion cap
    loosened and the adaptive rebuild cadence on, the simulated D-type
    growth curve still shows a gap to the analytic solution that
    neither fix closes. Both identified contributing factors (the cap,
    the fixed cadence) are confirmed real and exhausted as candidate
    root causes at their current settings -- the residual points at
    something else, not yet identified. A $T_i=10^4\,$K variant of the
    same combined setup passed the check outright, but three variables
    changed at once relative to the original mismatch, so $T_i$ is not
    cleanly isolated as the explanation. Not attempted: instrumenting
    which retry branch (buffer-full vs.\ radius-expansion) dominates
    once the cap/cadence fixes are both in place.

  \item[$\square$] \textbf{T4} -- User's stated direction (2026-07-19):
    the adaptive rebuild cadence (Section~\ref{sec:adaptive-cadence})
    is intended to become the \emph{default}, not stay opt-in
    indefinitely. \textbf{Deliberately not done yet} (2026-07-20): the
    user wants more testing first, so
    \code{HII\_adaptive\_rebuild\_cadence} stays off by default.
    \textbf{TODO (2026-07-24)}: the Smith et al.\ (2021) clump target
    (\code{StromgrenSphereClumpSmith2021}) has never actually been run
    with \code{HII\_adaptive\_rebuild\_cadence=1} -- every clump run to
    date (SPH uniform/angular, both resolutions, Gizmo MFM, Gizmo MFV)
    used the fixed cadence (\code{=0}). STARBENCH's and
    \code{StromgrenSphereHuSmith2017}'s own committed \code{run.sh}
    already default to \code{=1} (their comments cite an untracked
    ``T11'' with no corresponding entry in this document -- a dangling
    reference worth resolving, not a real cross-check), so the clump
    target is the one validated example still untested under adaptive
    cadence -- add it to the planned \code{HomogeneousBox}/isolated-galaxy
    testing pass below.

  \item[$\blacksquare$] \textbf{T5} -- Pass real ionizing/heating rates
    into Grackle's chemistry solve (Grackle's own \code{RT\_heating\_rate}/
    \code{RT\_HI\_ionization\_rate} fields), instead of this model's
    instant flag-and-floor scheme, via
    \code{GEARFeedback:HII\_couple\_ionization\_rate}. Implemented and a
    caching bug that left \code{RT\_heating\_rate} always exactly zero
    fixed -- see Section~\ref{sec:t5-heating-rate-zero}. A sanity check
    against the cheapest validation target
    (\code{StromgrenSphereClumpSmith2021}) now holds near-equilibrium
    ionization for the full run; the three STARBENCH/Hu-Smith/clump
    targets have not yet been re-validated with this feature on (it
    remains off by default, so their existing PASS status is unaffected
    either way). User's explicit position (2026-07-19): worth testing,
    \emph{not} for the current STARBENCH validation effort (both
    \citet{Hu2017} and \citet{Smith2021} themselves use the same
    flag-and-floor approach this model does, so rate-coupling solves a
    different problem than the one being validated against), and not
    before it -- a standalone follow-up. See memory
    (\code{reference\_gizmo\_fire\_rt\_rate\_coupling.md}) for how
    FIRE-3 actually does this (self-consistent solve, Stromgren-value
    incident flux, self-shielding off) and why it needs an actual local
    radiation field, not just a number handed to Grackle. Grackle also
    has an equally-real, separate coupling point for H$_2$
    photodissociation and for photoelectric heating -- nothing needs
    changing inside Grackle for either channel; the entire gap is the
    variable-ISRF work tracked entirely in the dedicated Radiation
    Pressure \& ISRF logbook (\code{radiation\_pressure\_isrf.tex}/
    \code{.pdf}, this same directory).

  \item[$\square$] \textbf{T7} -- Extend \texttt{pychem}
    (\texttt{\textasciitilde/programs/pychem/}, already the source of
    this project's GEAR chemical-yield tables) to emit proper
    $(M,Z)$-dependent stellar radius/luminosity/effective-temperature
    (and hence ionizing photon rate) tables, replacing the current
    single-metallicity, mutually-disagreeing piecewise power-law fits
    in \code{radiation.c} (Section~\ref{sec:blackbody} documents the
    disagreement in detail). A standalone project, not a small in-place
    fix. When it lands, delete the dead
    \code{radiation\_get\_individual\_star\_temperature}
    (Section~\ref{sec:blackbody}). \textbf{Scope widened (2026-07-20)}:
    $L_{\rm FUV}$ and $L_{\rm LW}$ (two more explicit per-star band
    luminosities, decided in the Radiation Pressure \& ISRF logbook)
    are two more band integrals of the same underlying stellar-atmosphere
    model this project would tabulate -- not a separate
    table-generation effort from $Q_H$/$L_{\rm bol}$, just two more
    threshold pairs on the same curve.

  \item[$\square$] \textbf{T9} -- \code{space\_regrid}'s top-level
    cell grid never re-refines once coarsened, even after the
    \code{h\_hii} that coarsened it shrinks back down (e.g.\ a star
    dies). Traced in code (2026-07-20), not yet fixed.
    \textbf{Per-star and per-cell bookkeeping is already correct}:
    \code{feedback\_common.c:372-378} explicitly resets
    \code{sp->h\_hii = 0.0} when a star dies or ages out of HII
    eligibility (comment: ``This prevents dead particles with large
    h\_hii to force the tasks at higher levels than necessary'',
    final value preserved in
    \code{sp->tracers\_data.final\_HII\_radius}), and
    \code{c->stars.h\_hii\_max} (the per-cell aggregate
    \code{space\_regrid} reads, Section~\ref{sec:space-regrid-hhii}) is
    recomputed fresh from current \code{sparts[i].h\_hii} at every
    tree rebuild/drift (\code{cell.c}, \code{cell\_drift.c},
    \code{space\_split.c}) -- so it does shrink correctly once a
    star's \code{h\_hii} resets. \textbf{The gap is one level up}:
    \code{space\_regrid.c}'s own regrid trigger,
    \begin{quote}
    \code{if (s->cells\_top == NULL || cdim[0] < s->cdim[0] || ...)}
    \end{quote}
    only fires when the newly-computed \code{cdim} is \emph{smaller}
    (coarser) than the grid's current \code{cdim} -- never when it
    would come out larger (finer) again. This is a general,
    pre-existing SWIFT property, true for ordinary hydro \code{h\_max}
    too, not introduced by this branch: once cells coarsen, they stay
    coarse for the rest of that run, regardless of why \code{h\_max}
    grew. \code{h\_hii} is the first mechanism in this codebase where a
    \emph{single, brief, transient} event (one star's HII region,
    possibly capped only by the periodic floor of 3,
    Section~\ref{sec:space-regrid-hhii}) can trigger this, unlike ordinary
    hydro resolution needs which are usually sustained, not transient.
    \textbf{Practical concern}: in a real multi-star simulation, one
    star's HII region growing large enough to coarsen \code{cdim} --
    even briefly, even down to the periodic floor -- would coarsen the
    whole box for the remainder of that run, even after that star dies
    and every other star stays small.

    \textbf{Decision (2026-07-21)}: scoped to h\_hii specifically, not a
    general change to the one-way coarsening ratchet ordinary
    hydro/star/black-hole/sink \code{h\_max} still relies on (ratchet
    behaviour for those is a pre-existing, general SWIFT property, out
    of scope for this branch). \code{space::h\_max\_no\_hii\_hwm}
    (\code{src/space.h}) is a new persistent high-water mark that
    reproduces the existing ratchet exactly for the non-\code{h\_hii}
    contributions to \code{h\_max}; \code{h\_hii}'s own contribution is
    recomputed fresh every regrid (no memory), and the regrid trigger in
    \code{space\_regrid.c} is now bidirectional (\code{cdim != s->cdim}
    instead of \code{cdim < s->cdim}). This is safe because \code{cdim}
    can only come out \emph{larger} (finer) than before when
    \code{h\_hii}'s contribution has shrunk -- the ratchet guarantees the
    ordinary-physics component never causes that direction on its own.
    Implemented in \code{src/space.h}/\code{src/space\_regrid.c}; builds
    clean (\code{swift}/\code{swift\_mpi}) under
    \code{-Werror -Wall -Wextra -Wshadow}. \textbf{Committed
    (2026-07-21)}: \code{testRadiationRebuildCheck} and
    \code{test27cellsStars} both pass, and a full STARBENCH re-run
    ($T_i=10^4\,$K forced, the same edge case that pins
    \code{max\_top\_level\_cells: 3} at the periodic boundary) completed
    end to end at $9.23\%$ (vs.\ $6.12\%$ pre-change -- within the
    scatter expected from this example's probabilistic boundary
    ionization, not attributed to a regression).

    \textbf{Complementary general safety, also added}: a new
    \code{Scheduler:min\_top\_level\_cells} parameter (default 3,
    mirroring the existing \code{Scheduler:max\_top\_level\_cells}) sets
    \code{space::cell\_max\_width}, a ceiling on the effective cell width
    that \code{space\_regrid()} will not coarsen past regardless of what
    is driving the coarsening (ordinary \code{h\_max} or \code{h\_hii}).
    This bounds the worst case even if the bidirectional trigger above
    never actually gets exercised in a given run.

    \textbf{Explicitly unverified}: whether the bidirectional trigger is
    reachable in practice for a continuously star-forming cosmological
    box (as opposed to quiescent periods, isolated-galaxy/zoom tests, or
    bursty SF) is an untested hypothesis, not a measured result -- no run
    has yet checked whether \code{cdim} actually gets a chance to shrink
    back in a continuous-SF setting. The STARBENCH re-run that
    accompanied the 2026-07-21 commit above did not exercise this either:
    STARBENCH's single star never dies within the run's $\sim$3\,Myr
    duration (far short of its lifetime), so \code{h\_hii} never shrinks
    back down and the re-refine path was likely never triggered -- that
    run only confirmed no regression, not that the fix's core mechanism
    works.

    \textbf{TODO (added 2026-07-23, from a direct question about whether
    this had ever actually been checked): explicitly verify the
    coarsen-then-re-refine cycle}, in two places:
    \begin{itemize}
      \item \code{HomogeneousBox} (T4's planned isolated-galaxy/
        continuous-SF test) -- as originally planned above.
      \item \textbf{StromgrenSphere} -- a single star whose \code{h\_hii}
        grows then shrinks back to zero (e.g.\ by running well past that
        star's lifetime, or reusing \code{HII\_max\_age\_Myr} to force
        early HII shutoff) is a much cheaper, more controlled way to
        directly exercise both directions of the trigger than a full
        continuous-SF box -- worth doing \emph{first}, before the
        more expensive \code{HomogeneousBox} test.
    \end{itemize}
    In both cases, check \code{cdim}/top-level cell count directly
    (e.g.\ via logged regrid messages or \code{task\_level}/dependency-graph
    output showing the grid actually refining back to more cells) rather
    than only checking the final physics result, since a physics-level
    check alone would not distinguish ``re-refined correctly'' from
    ``never needed to re-refine.''

  \item[$\square$] \textbf{T10} -- STARBENCH and Hu et al.\
    (2017)/Smith et al.\ (2021) 4-star are both validated (PASS),
    Sections~\ref{sec:starbench-current}/\ref{sec:husmith-current}.
    \textbf{Still open}: whether matching STARBENCH's flat-$T_i$
    convention should become a default-buildable option (not just a
    debug flag), or whether this code's natural, metallicity-dependent
    $T_i$ is the intended behaviour and STARBENCH/Hu-Smith comparisons
    are understood to always require the explicit build -- a design
    decision, not a reproducibility gap (both examples' READMEs
    document the flag).
\end{itemize}
